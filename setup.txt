Set internet to connect on startup
usermod -aG wheel username

1. Install alacarte
# sudo yum install alacarte

2. Install atom
# sudo yum install lsb-core-noarch
# sudo yum install libXScrnSaver
# pip install pycodestyle
# rpm -Uvh atom.x86_64.rpm
# set pycodestyle path in Linter prefs
# set sync-settings plugin
  Access: SEE GITHUB DEVELOPER SETTINGS
  GIST ID; 3c7678b1c60c348f0266567e44dad0fa

3. Gnome tweak tools for 4k
# yum install gnome-tweaks
Global Dark Theme On
Fonts scaling 1.5

4. Gnome settings for 4k
Universal Access->Large Text ON

5. Mount drives
# sudo blkid
# vi /etc/fstab
# sudo mkdir /scratch0
# sudo mkdir /scratch1
# mount -a

6. Emulate middle click
# touch /etc/X11/xorg.conf.d/20-3rdbutton.conf

Add in:

Section "InputClass"
  Identifier "evdev pointer catchall"
  MatchIsPointer "on"
  MatchDevicePath "/dev/input/event*"
  Option "Emulate3Buttons" "True"
  Option "Emulate3Timeout" "100"
  Driver "evdev"
EndSection

Section "InputDevice"
        Identifier      "Configured Mouse"
        Driver          "mouse"x
        Option          "CorePointer"
        Option          "Device"                "/dev/input/mice"
        Option          "Protocol"              "ImPS/2"
        Option          "Emulate3Buttons"       "true"
EndSection

7. Make home dir symlinks for my data drive
# cd ~
# rmdir Downloads
# ln -s /scratch0/NOT_BACKED_UP/dbuchan/Applications Applications
# ln -s /scratch0/NOT_BACKED_UP/dbuchan/Downloads Downloads
# ln -s /scratch0/NOT_BACKED_UP/dbuchan/Code Code
# ln -s /home/dbuchan/Jalview/Jalview runJalview

8. Restore backed up home/ from data drive
# cp -r .atom ~/
# cp -r bin ~/
# cp -r JabRef/ ~/
# cp -r Jalview ~/
# cp -r R/ ~/
# cp /scratch0/NOT_BACKED_UP/dbuchan/dbuchan/.ssh/* ~/.ssh
# cp /scratch0/NOT_BACKED_UP/dbuchan/dbuchan/.pyirc ~/

9. Enable EPEL
# sudo yum install https://dl.fedoraproject.org/pub/epel/epel-release-latest-7.noarch.rpm

10. Install keepass
# sudo yum install keepass

11. Install Dropbox and log in
GLIBC2.18 SPOOFING NO LONGER WORKS FOR LATEST HEADLESS DROPBOX
https://www.dropbox.com/en_GB/install-linux
# cd ~ && wget -O - "https://www.dropbox.com/download?plat=lnx.x86_64" | tar xzf -
# cd .dropbox-dist
# echo fs.inotify.max_user_watches=100000 | sudo tee -a /etc/sysctl.conf; sudo sysctl -p
# dropboxd
https://askubuntu.com/questions/315034/start-dropbox-on-startup

https://lorenzo.mile.si/dropbox-on-centos7-requires-glibc-2-19/828/

12. Update firefox to local
# cd /usr/share/applications
# sudo vi firefox.desktop
update to Exec=/scratch0/NOT_BACKED_UP/dbuchan/Applications/firefox/firefox %u
# sudo chattr +i firefox.desktop

# sudo mv /bin/firefox /bin/firefox_old
# sudo ln -s /scratch0/NOT_BACKED_UP/dbuchan/Applications/firefox/firefox /bin/firefox


13. Log in to web sites mail.com, gmail, firefox, reddit, gwj, github

14. get slack
Could skip given Franz install below
https://slack.com/downloads/linux
https://wiki.base22.com/btg/install-slack-in-centos-7-133993146.html
# sudo yum install libappindicator
# sudo yum -y install /home/dbuchan/Downloads/slack-3.3.8.rpm
14. Log slack in to psipred, ucl-programming-hub, crickcomputingchat

15. get thunderbird
# sudo yum install thunderbird

15. Set up thunderbird for crick and UCL email accounts

16. Setup git
# sudo yum install git
https://help.github.com/articles/setting-your-username-in-gitp

17. Install python3
# sudo yum install python36-x86_64
# sudo yum install python36-devel

18. Install google-chrome
https://www.if-not-true-then-false.com/2010/install-google-chrome-with-yum-on-fedora-red-hat-rhel/

19. Install virtualenv for local python3
# sudo yum install python36-pip
# /scratch0/NOT_BACKED_UP/dbuchan/python3/bin/pip3 install virtualenv

20. Make postgres dir writeable by other Users
# sudo chmod uog+rw /var/run/postgresql/

21. Run through aa install to ensure redis is working for web dev
# yum install redis

22. add /opt config (also for web dev)
# sudo ln -s /scratch0/NOT_BACKED_UP/dbuchan/Code/ Code
# sudo ln -s /scratch0/NOT_BACKED_UP/dbuchan/Applications Applications
# sudo ln -s /scratch1/NOT_BACKED_UP/dbuchan/ Data

23. Set up npm for psipred_javascript
# sudo yum install npm

24. Install connectTunnel for CS dept VPN
http://tsg.cs.ucl.ac.uk/basics/vpn/#c26036
https://www.sonicwall.com/en-us/support/technical-documentation/secure-mobile-access-12-0-connect-tunnel-user-guid/connect-tunnel-client-for-mac-linux

25. Install ffmpeg (to allow h264 playback in firefox)
https://www.vultr.com/docs/how-to-install-ffmpeg-on-centos
# sudo rpm --import http://li.nux.ro/download/nux/RPM-GPG-KEY-nux.ro
# sudo rpm -Uvh http://li.nux.ro/download/nux/dextop/el7/x86_64/nux-dextop-release-0-5.el7.nux.noarch.rpm
# sudo yum install gnutls-devel
# sudo yum clean all
# sudo yum install ffmpeg ffmpeg-devel ffmpeg-libs -y

26. install gnuplot
# sudo yum install gnuplot1
# sudo yum install ghostrscript

27. Get 32-bit linux libs
# sudo yum install glibc.i686

28. Install gnome-keyring for github key management
https://stackoverflow.com/questions/13385690/how-to-use-git-with-gnome-keyring-integration
# sudo yum install gnome-keyring, libgnome-keyring.x86_64, libgnome-keyring-devel.x86_64,  libgnome-keyring.i686, libgnome-keyring-devel.i686
# cd /usr/share/doc/git-1.8.3.1/contrib/credential/gnome-keyring
# sudo make
# git config --global credential.helper /usr/share/doc/git-1.8.3.1/contrib/credential/gnome-keyring/git-credential-gnome-keyring

29. Install openjdk
# sudo yum install java-1.8.0-openjdk, java-1.8.0-openjdk-devel

30 Install Gnome extensions
# sudo yum install git cmake coreutils pygobject2 python-requests jq
# sudo yum install gnome-shell-browser-plugin
# sudo yum install gnome-shell-extension-common
# sudo yum reinstall nss-util
Open Gnome-tweak-tool->Extension->Get More extensions
Note gnome browser extension only seems to work in Chrome
Get "Frippery Panel Favourites" and whatever

31. Install gnome pomodoro
https://github.com/codito/gnome-pomodoro/issues/361
# sudo yum install autoconf-archive
# sudo yum install libappstream-glib-devel
# sudo yum install gom-devel
# sudo yum install libpeas-devel
# sudo yum install sqlite-devel
# sudo sudo yum install gnome-common intltool vala vala-tools glib2-devel gobject-introspection-devel gtk3-devel gnome-desktop3-devel libcanberra-devel dbus-glib-devel gstreamer1-devel telepathy-glib-devel google-droid-sans-fonts
# git clone -b gnome-3.28 https://github.com/codito/gnome-pomodoro.git
# cd gnome-pomodoro
# ./autogen.sh --prefix=/usr --datadir=/usr/share
   (needs to run twice to make to build)
# make
# sudo make install
# sudo cp /usr/lib/libgnome-pomodoro.* /usr/lib64
http://gnomepomodoro.org/

32. Install perl packages
# sudo cpan install Expect
may require force on some systems

33. Install kmod-nvida
Install elrepo
https://elrepo.org/tiki/tiki-index.php
then
# sudo yum install kmod-nvidia

34. Install postresql things and  /var/run/postgres world writeable
# sudo yum install stgresql-devel
https://serverfault.com/questions/699307/how-to-grant-access-to-var-run-to-postgres-on-centos
# sudo chmod +x /etc/rc.d/rc.local
add to rc.local
chmod uog+rw /var/run/postgres
edit /usr/lib/tmpfiles.d/postgresql.conf
set permission to 0777

35. Install gd
# sudo yum install gd, gd-devel
# sudo cpan install GD

36. Ugly X hack to stop nvidia driver rewriting conf on boot
# sudo chmod u-w xorg.conf
# sudo chattr +i /etc/X11/xorg.conf

37. Update editor
# sudo yum install gconf-editor

38. Update users logon to prevent suspend/hibernate
User -> Account Settings
set Automatic Log in to ON
Settings -> Power
Blank Screen Never

39. R/Rstudio Setup
sudo yum install R
sudo yum install readline-devel
get free https://www.rstudio.com/products/rstudio/download/
cd /usr/share/applications
sudo touch rstudio.desktop
sudo vi rstudio.desktop
add:
[Desktop Entry]
Name=Rstudio
GenericName=IDE
Comment=IDE for Rstudio
Exec=/scratch0/NOT_BACKED_UP/dbuchan/Applications/rstudio-1.1.456/bin/rstudio
Terminal=false
Type=Application
Icon=/scratch0/NOT_BACKED_UP/dbuchan/Applications/rstudio-1.1.456/rstudio.png
Categories=GNOME;GTK;Utility;TextEditor;Development;
StartupNotify=true

40. Install Inkscape
sudo  yum install inksacpe

41. Install gv
sudo yum install gv

42 Install Franz
https://meetfranz.com/
see github releases for tarball
cd /usr/share/applications
sudo touch franz.desktop
sudo vi franz.desktop
add:
[Desktop Entry]
Name=Franz
GenericName=Media
Comment=Social Media things
Exec=/scratch0/NOT_BACKED_UP/dbuchan/Applications/franz-5.0.1/franz
Terminal=false
Type=Application
Icon=/scratch0/NOT_BACKED_UP/dbuchan/Applications/franz-5.0.1/index.svg
Categories=GNOME;GTK;Utility;
StartupNotify=true

43 Install httpd
# sudo yum install httpd
# sudo yum install httpd-devel

44 Check that any legacy virtualenvs do or don't need rebuilding
# rm -rf [ENV]
# mkvirtualenv [ENV]
# pip install -r requirements/[SOMETHING].txt
